import { Center } from '@chakra-ui/react';
import OfflineShop from '~/UI/OfflineShop';

const ShopsPage = () => {
	return (
		<Center pt={[100, 150]} pb={75}>
			<OfflineShop />
		</Center>
	);
};

export default ShopsPage;
